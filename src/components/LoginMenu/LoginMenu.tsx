import {Link} from "react-router-dom";

const LoginMenu = () => {
    return (
        <div>
            <Link to='/login'> Click here for login</Link>
        </div>
    );
};

export default LoginMenu;